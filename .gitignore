# Build artifacts
tools/**/bin/
tools/**/obj/

# Working scratch — loose game-derived assets stay out of the repo
work/
*.uasset
*.uexp
*.ubulk
*.usmap
scriptobjects.bin
oodle-data-shared.dll

# NOTE: dist/ (the built mod .pak/.ucas/.utoc + zips) IS tracked for distribution — see README.
